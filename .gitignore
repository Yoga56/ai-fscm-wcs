node_modules/
app/cfobrief/dist/
app/cfobrief/mock/data/generated/
.DS_Store
DUMP/

# On the "ui" branch only (this one): app/ and docs/ are tracked here so
# Business Application Studio can clone this branch and deploy the UI5
# app. They stay gitignored on "main", which is the abapGit-linked
# branch - a non-ABAP folder there breaks FOLDER_LOGIC=PREFIX.
tools/
.claude/
PASTE_CHECKLIST.md
