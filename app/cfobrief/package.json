{
  "name": "cfobrief",
  "version": "1.0.0",
  "description": "AI-Enabled CFO Daily Brief - freestyle SAPUI5 cockpit on a RAP OData V4 service",
  "private": true,
  "sapux": false,
  "scripts": {
    "mock:data": "node mock/generate.js",
    "start-mock": "npm run mock:data && ui5 serve --config ui5-mock.yaml --port 8080 --open index.html",
    "serve-mock": "npm run mock:data && ui5 serve --config ui5-mock.yaml --port 8080",
    "start": "ui5 serve --config ui5.yaml --port 8081 --open index.html",
    "test": "node --test test/*.test.js",
    "lint": "ui5lint",
    "build": "ui5 build --config ui5-deploy.yaml --clean-dest",
    "deploy": "npm run build && fiori deploy --config ui5-deploy.yaml"
  },
  "devDependencies": {
    "@sap-ux/ui5-middleware-fe-mockserver": "^2",
    "@sap/ux-ui5-tooling": "^1",
    "@ui5/cli": "^4",
    "@ui5/linter": "^1"
  }
}
